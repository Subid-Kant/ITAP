# ITAP v2.0 — Backend Dependencies
# Production-ready dependency set (relaxed pins for Python 3.14 compat)

# ── Core Framework ───────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.1
python-dotenv>=1.0.1
pydantic>=2.7.4
pydantic-settings>=2.3.4
python-multipart>=0.0.9

# ── Database ─────────────────────────────────
sqlalchemy>=2.0.31
greenlet>=3.0.0            # Required by SQLAlchemy async engine
aiosqlite>=0.20.0          # SQLite async driver (dev/demo)
asyncpg>=0.29.0            # PostgreSQL async driver (production)
alembic>=1.13.1

# ── Security & Auth ──────────────────────────
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# ── OSINT APIs ───────────────────────────────
shodan>=1.31.0
requests>=2.32.3
aiohttp>=3.9.5
httpx>=0.27.0

# ── ML/AI ────────────────────────────────────
numpy>=1.26.4
pandas>=2.2.2
scikit-learn>=1.5.1
scipy>=1.13.1
h5py>=3.11.0

# ── WebSocket ────────────────────────────────
websockets>=12.0

# ── Email ────────────────────────────────────
aiosmtplib>=3.0.1          # Async SMTP (optional, for production email)

# ── Utils ────────────────────────────────────
jinja2>=3.1.4
psutil>=5.9.0              # System/process monitoring
